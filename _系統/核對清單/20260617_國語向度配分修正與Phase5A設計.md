# 工作紀錄：國語向度配分修正 + Phase 5A 相容性約束表設計

- 日期：2026/6/17
- 範圍：Phase 1 部分實作（已驗證）+ Phase 5A 設計封盤（純文件）
- 操作者：Claude（實作與文稿；未代為 commit／push／deploy）

> ⚠️ 待查證（2026/6/17）：使用者本機 `git status -sb` 顯示工作樹乾淨、僅 `docs/Phase5_…md` 未追蹤，無 ahead commit。
> 故「Phase 1 改動是否已 commit／push 進本機 repo」**尚未經 git log 證實**。
> 已確定：Phase 1 程式碼存在於工作樹，且本機 `npm test` 曾得 92 綠。
> 未證實（推論）：協作模型的 errorChange 修正 push 是否含本批改動、是否已併入本機 HEAD。
> 處置：先以本機 `git log --oneline -8` 對齊真實狀態，再決定提交策略；在對齊前不進 Phase 5A／5B。

## 一、已改動並驗證的程式（Phase 1 部分）

1. `frontend/src/app.js`
   - 配題分布表改從題位（intents）重算，修正「72 ≠ 100」（題組展開後配分正確），並加「合計」列。
   - 新增 `computeTargetScores`：國語走向度比例、其他科走節數比例（封裝分流，為 Subject Profile 鋪路）。
   - 目標配分預覽：國語欄位「授課節數」改「評量向度」，合計列節數隱藏。
2. `frontend/src/core/periods.js`
   - 新增純函式 `computeChineseDimensionScores`：兩層最大餘數法（向度比例 → 向度內細項均分），總分守恆；教師仍可逐項覆寫。
3. `tests/periods.test.js`
   - 新增 3 個單元測試（向度比例分配、最大餘數補足、無比例退回等權）。

驗證：本機 `npm test` 92 綠、`npm run check` 通過、`node --check` 無誤（使用者機器為權威）。

## 二、配分落差（實際 vs 預估）根因確認

- 預估配分（向度比例）正確。
- 實際配分取自「題目 primaryObjectiveId」加總；舊版 `distributeObjectivesToSlots` 用「絕對誤差平方最小」→ 大目標被餓死（句型 17 只拿到 5）。
- 協作模型回報已將該演算法修正為 `errorChange = newError − currentError`（最大缺口優先），並回報 push（93 測試）。**此 push 是否含本批 Phase 1 改動、是否已進本機 HEAD，待 git log 證實**。沙箱診斷（內嵌真檔邏輯）確認 errorChange 版向度守得住（句式 16/16/16）。
- 注意：使用者截圖的落差來自「舊藍圖」生成的卷，須**重新生成國語卷**再驗；若仍有殘餘，才進 Phase 5B。

## 三、產出文件

1. `docs/設計備忘_全科統一模型與向度驅動_20260617.md`（v3 封盤）：題位中心、Subject Profile、三層模型、落地順序、abilityId 穩定鍵。
2. `docs/Phase5_細項題型相容與能力驅動配題_20260617.md`（Phase 5A 封盤）：相容性約束表（carrier/response 拆分、meta 表推導旗標、selectionPolicy、Helper 行為約定、42 細項相容表草稿、12 條測試、Codex 任務、拍板結論）。
3. 各版草稿備份於 `_備份/`（設計備忘 v1–v2、Phase5 v1–v4）。

## 四、安裝的套件（沙箱，供測試用）

- 沙箱 Linux：`vitest@3`（於 /tmp 乾淨複本跑單元測試，因掛載讀取會截斷大檔，不可直接在掛載上跑）。未在使用者 Windows 安裝任何套件。

## 五、查證事項

- 國語細項數：經程式攤平驗證為 **42**（非先前誤寫的 49），全部唯一無同名。

## 六、待辦（不需 Claude 立即動程式）

1. 封盤現有孤兒題／timeout 修補與 Phase 1 改動，確認 `git status` 乾淨（commit／push 由使用者執行；本機已裝 Git）。
2. 重新生成國語卷，驗「實際 vs 預估」是否回到約 30/50/20。
3. 將 Phase 5A 第四節相容表帶去 Gemini／NotebookLM 與教學現場校核（重點：✔ 細項、圖表邊界、competency_set／short_answer 範圍）。
4. 表校定後 → 交 Codex 做 Phase 5A 純資料＋helper＋測試（不得接 allocator／prompt／UI）。
5. 驗收後 → Phase 5B：`targetScore → slot allocator → slots`，真正解配分落差。

## 七、開工序（鐵則）

封盤現有修補 → 確認乾淨樹 → Phase 5A 純資料＋測試 → Phase 5B allocator。不得把新重構疊在未提交修補上。
